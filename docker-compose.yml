services:
  backend:
    build: ./backend
    container_name: domohub-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - domohub-data:/app/data
    environment:
      - DEBUG=false
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - MQTT_ENABLED=${MQTT_ENABLED:-false}
      - MQTT_HOST=${MQTT_HOST:-mosquitto}
      - MQTT_PORT=${MQTT_PORT:-1883}
    networks:
      - domohub-net

  frontend:
    build: ./frontend
    container_name: domohub-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - domohub-net

  mosquitto:
    image: eclipse-mosquitto:2
    container_name: domohub-mqtt
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto-data:/mosquitto/data
    networks:
      - domohub-net
    profiles:
      - mqtt

volumes:
  domohub-data:
  mosquitto-data:

networks:
  domohub-net:
    driver: bridge
