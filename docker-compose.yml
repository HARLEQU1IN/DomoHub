services:
  backend:
    build: ./backend
    container_name: marsflow-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - marsflow-data:/app/data
    environment:
      - DEBUG=false
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - MQTT_ENABLED=${MQTT_ENABLED:-false}
      - MQTT_HOST=${MQTT_HOST:-mosquitto}
      - MQTT_PORT=${MQTT_PORT:-1883}
    networks:
      - marsflow-net

  frontend:
    build: ./frontend
    container_name: marsflow-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - marsflow-net

  mosquitto:
    image: eclipse-mosquitto:2
    container_name: marsflow-mqtt
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto-data:/mosquitto/data
    networks:
      - marsflow-net
    profiles:
      - mqtt

volumes:
  marsflow-data:
  mosquitto-data:

networks:
  marsflow-net:
    driver: bridge
